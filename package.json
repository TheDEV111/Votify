{
  "name": "votify-dao",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "contracts-clarinet"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "contracts:check": "npm run check --workspace=contracts-clarinet",
    "contracts:test": "npm run test --workspace=contracts-clarinet"
  }
}
